import { scale, moderateScale, verticleScale } from './scale';
export default Paddings = {
    paddings: {
        xs: moderateScale(3),
        s: moderateScale(6),
        m: moderateScale(12),
        l: moderateScale(20),
        xxl: moderateScale(300),
    },
};