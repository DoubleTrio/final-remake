import { scale, moderateScale, verticleScale } from './scale';
export default FontSizes = {
    fontSizes: {
        header: moderateScale(18),
        m: moderateScale(16),
        s: moderateScale(11),
    }
};